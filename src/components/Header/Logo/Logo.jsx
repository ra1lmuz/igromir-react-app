import logo from "../../../assets/logo.svg"

const Logo = () => {
    return (
        <div className="logo">
            <img src={logo} alt="IgroMir" />

            <div>
                <h1>IgroMir</h1>
            </div>
        </div>
    )
}

export default Logo;